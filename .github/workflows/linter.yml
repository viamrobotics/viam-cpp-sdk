name: cpp-linter

on: pull_request

jobs:
  cpp-linter:
    if: github.repository_owner == 'viamrobotics'
    runs-on: [self-hosted, x64]
    container:
      image: ghcr.io/viamrobotics/canon:amd64
    steps:
      - name: Checkout PR/Push/Workflow Dispatch
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - name: Run linter
        run: ./bin/run-clang-format.sh
